// -------------------- Caso 1 --------------------

const numero = promt ("Ingresa el numero que deseas evaluar");
for (numero = 1; i <= n; i++) {
    document.write(i + " — Cuadrado: " + (i*i) + " — Cubo: " + (i*i*i) + "<br>");
}
